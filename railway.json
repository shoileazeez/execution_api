{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "PYTHON",
      "buildCommand": "python -m venv --copies /opt/venv && source /opt/venv/bin/activate && pip install -r requirements.txt"
    },
    "deploy": {
      "startCommand": "source /opt/venv/bin/activate && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn execute.wsgi"
    }
  }